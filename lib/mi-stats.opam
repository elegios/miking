# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
synopsis: "Miking's numerics support library"
description: """
Bindings to a pared-down, vendored Stan Math (plus the Boost and Eigen
subsets it needs) backing Miking's probabilistic and matrix externals.
Replaces the previous dependency on owl."""
depends: [
  "dune" {>= "3.5"}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
